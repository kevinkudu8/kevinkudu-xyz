# Copy to .env.local and fill in. Never commit real values.
# Airtable personal access token (server-side only — never prefix with NEXT_PUBLIC_)
AIRTABLE_TOKEN=
# The base ID for the shelf base, e.g. appXXXXXXXXXXXXXX
AIRTABLE_BASE_ID=
